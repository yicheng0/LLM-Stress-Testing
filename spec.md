# 供应商模型接入模板与测试任务中心 Spec

## 背景

当前系统已经支持供应商与官方参考接口的 Token、缓存 Token、延迟和目录计价自测，也可以查看单次任务运行结果。

目前每次发起自测都需要重新填写供应商接口、模型、参考模型、Token 测试组和计费规则，测试任务列表也主要按通用压测任务展示，不能完整体现供应商接入验收状态。因此需要先建立可复用的模型接入模板，并将测试任务列表调整为面向供应商准入的任务中心。

## 目标

- 让用户能够保存、查看和复用供应商模型接入自测模板。
- 让用户可以基于模板快速发起一次供应商接入自测，只补充本次临时凭证。
- 让测试任务列表清晰展示供应商、模型、Token 核验和计费核验状态。
- 保留现有通用压测、缓存专项测试和已完成自测结果的兼容性。
- 对缺少 usage、价格来源或缓存计价信息的场景明确显示“无法核验”。

## 功能需求

- F1: 用户可以创建供应商模型接入模板，填写模板名称、供应商名称、模型名称、API 协议、供应商接口地址、官方参考模型、输入 Token 测试组、最大输出 Token、流式测试开关、缓存测试开关、Token 容差和计费规则。
- F2: 用户可以查看模板列表，看到模板名称、供应商、模型、协议、版本、启用状态和更新时间。
- F3: 用户可以编辑模板并启用或停用模板；停用模板不能用于新建测试，但不影响已经创建的历史任务和结果。
- F4: 用户可以从启用的模板发起供应商接入自测，并在发起时输入本次临时 API Key；API Key 只能用于本次请求，不进入模板、任务详情、历史记录、报告或前端持久化数据。
- F5: 发起测试时系统使用模板保存的参数创建任务，并继续执行现有的供应商与官方参考接口成对测试流程。
- F6: 测试任务列表支持展示任务编号、任务状态、测试模板、供应商、模型、协议、总 Case、成功数、失败数、Token 结论、计费结论、开始时间和操作入口。
- F7: 测试任务列表区分通用压测任务、缓存专项测试任务和供应商接入自测任务；供应商自测任务不展示无意义的并发、成功率或 TPM 字段。
- F8: 用户可以从测试任务列表进入任务运行页、测试结果页和 Case 明细页；已有历史任务的详情入口保持可用。
- F9: 测试结果和任务列表必须区分“通过”“异常”“无法核验”；输出 Token 数量差异只能作为观察信息，不能单独导致供应商自测失败。
- F10: 价格规则必须保留来源、版本和生效时间；没有可信价格来源或必要价格字段时，相关计费结论显示“无法核验”，不得以 0 代替缺失值。
- F11: 模板和任务配置不得持久化完整 Prompt、完整请求体、完整响应体、API Key 或其他认证信息；报告和日志同样不得包含这些敏感数据。

## 非功能需求

- N1: 现有登录、权限、任务状态、报告下载和结果保留策略继续有效。
- N2: 模板字段校验应在提交前阻止空值、非法 Token 长度、非法容差、无效 URL 和未配置价格规则。
- N3: 页面应能明确提示当前价格目录是示例价格还是已验证官方价格，避免将示例价格误认为官方报价证明。
- N4: 现有后端测试和前端生产构建不能因本功能回归失败。
- N5: 不使用真实供应商 API Key 做自动化回归；功能验证使用本地或可控的模拟接口。

## 不做的事

- 不在本阶段实现供应商真实账单、余额、账户流水或实际扣费对账。
- 不在本阶段实现复杂的供应商组织、联系人审批和多级权限体系。
- 不在本阶段实现自动同步官方价格；价格仍由受控目录维护，并标记来源和验证状态。
- 不在本阶段重构现有通用压测任务的数据模型或报告体系。
- 不在本阶段实现定时复测、自动准入、审核记录和已准入模型管理；这些作为后续阶段。

## 验收标准

- AC1: 创建一个包含合法字段的模板并保存后，刷新页面仍能看到模板名称、供应商、模型、协议、版本和启用状态。
- AC2: 使用已启用模板发起供应商自测时，页面自动带出模板配置，只要求输入本次临时 API Key；任务能够进入现有运行流程。
- AC3: 使用停用模板发起新测试时，系统拒绝创建任务，并明确提示模板不可用；该模板对应的历史任务仍可查看。
- AC4: 在测试任务列表中，供应商自测任务显示供应商、模型、Token 结论和计费结论，且不显示无意义的并发、成功率和 TPM 字段；通用压测任务的原有字段仍正常显示。
- AC5: 从任务列表进入供应商自测任务的运行页、结果页和 Case 明细页均能成功跳转，历史任务详情入口不受影响。
- AC6: 当供应商或官方参考接口缺少 usage、total Token、价格字段或缓存价格时，结果显示“无法核验”，而不是显示通过或将缺失值按 0 计算。
- AC7: 当供应商输入 Token 在容差内、但输出 Token 与官方参考接口不同，结果不会仅因输出 Token 差异判定失败，并会将输出差异标记为观察项。
- AC8: 模板、任务详情、浏览器持久化数据、日志和导出报告中均检索不到 API Key、完整 Prompt、完整请求体和完整响应体。
- AC9: 后端现有测试、供应商自测相关回归检查和前端生产构建全部通过。
- AC10: 使用本地模拟供应商与官方接口完成至少一条非流式和一条流式自测流程，任务状态、Token 结果、费用结果和报告下载均可观察验证。
