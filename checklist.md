# 供应商模型接入模板与测试任务中心 Checklist

> 每项均通过运行代码或观察行为验证。

## 验收标准映射

| 验收标准 | 对应检查范围 |
|---|---|
| AC1 | 模板创建、刷新后持久化和列表字段完整性 |
| AC2 | 启用模板带出配置并进入现有自测运行流程 |
| AC3 | 停用模板阻止新任务且不影响历史任务 |
| AC4 | 三类任务字段区分和供应商任务指标展示 |
| AC5 | 运行页、结果页、Case 明细和历史任务入口 |
| AC6 | usage、总 Token、价格和缓存价格缺失时无法核验 |
| AC7 | 输出 Token 差异仅作为观察项 |
| AC8 | API Key、Prompt、请求和响应的全链路脱敏 |
| AC9 | 后端回归、Python 编译和前端生产构建 |
| AC10 | 本地模拟接口的非流式与流式端到端流程 |

## 实现完整性

- [ ] 创建合法供应商模型接入模板后，模板列表显示模板名称、供应商、模型、协议、版本、启用状态和更新时间（验证：登录后创建模板并刷新模板列表，期望数据仍存在且字段完整）。
- [ ] 模板编辑成功后版本号递增，配置内容更新，历史任务仍保留原任务配置（验证：编辑模板前后比较版本和历史任务详情）。
- [ ] 模板可以启用和停用（验证：分别执行启用、停用操作，期望列表状态和接口返回一致）。
- [ ] 停用模板不能用于新建供应商自测（验证：选择停用模板发起测试，期望接口拒绝并显示模板不可用）。
- [ ] 启用模板可以用于发起供应商自测（验证：选择启用模板并输入本次凭证，期望任务进入 queued 或 running）。
- [ ] 模板页面和自测发起页校验协议、URL、输入 Token 测试组、Token 容差和计费规则（验证：输入空值、重复 Token、非法范围、无效 URL 和协议不匹配价格规则，期望提交被阻止并显示明确错误）。
- [ ] 供应商自测继续使用相同 Prompt 和请求参数分别访问供应商接口与官方参考接口（验证：本地模拟服务记录两侧请求摘要，期望输入组和测试参数一致，且不产生额外预检请求）。
- [ ] 供应商自测结果分别展示输入、输出、总 Token、缓存读写 Token、费用、延迟和 TTFT（验证：完成模拟自测后查看结果页和 Case 明细）。
- [ ] 输入 Token 在容差内且输出 Token 不同的 Case 不会仅因输出差异失败（验证：模拟供应商返回不同输出 Token，期望输入 Token 通过，输出差异显示为观察项）。
- [ ] 输入 Token 超出绝对和相对容差时标记 Token 异常（验证：模拟供应商返回超容差输入 usage，期望 Token 结论为异常）。
- [ ] 缺失 usage、total Token、价格字段或缓存价格时显示无法核验（验证：分别运行缺失字段模拟 Case，期望不显示通过、不以 0 代替缺失值）。
- [ ] 示例价格和已验证价格在页面上明确区分（验证：查看当前示例价格规则，期望显示未配置官方来源或未验证提示）。

## 任务中心与兼容性

- [ ] 任务列表区分通用压测、缓存专项测试和供应商接入自测（验证：准备三类任务并打开历史任务列表，期望任务类型显示正确）。
- [ ] 供应商自测任务显示模板、供应商、模型、Case 统计、Token 结论和计费结论（验证：查看已完成供应商自测任务行）。
- [ ] 供应商自测任务不显示无意义的并发、成功率和 TPM 字段（验证：检查供应商任务表格列和详情字段）。
- [ ] 通用压测任务原有并发、成功率、TPM 和操作入口继续可用（验证：打开已有通用压测历史任务并进入报告）。
- [ ] 缓存专项任务原有缓存结果、详情和操作入口继续可用（验证：打开已有缓存专项历史任务并进入结果页）。
- [ ] 供应商任务可以从列表进入运行页、结果页和 Case 明细页（验证：分别点击三个入口，期望路由和页面内容正确）。
- [ ] 任务取消、历史记录和报告下载不受模板化改造影响（验证：对运行中的模拟任务执行取消，并下载 Summary JSON、Details JSONL、Markdown、HTML 和 PDF）。
- [ ] guest 和 root 用户遵守模板权限边界（验证：使用两个用户分别读取、编辑、启停模板，期望 guest 不能操作其他用户模板，root 可按既有权限管理）。

## 安全与数据留存

- [ ] 供应商 API Key 和官方参考 API Key 只存在于本次发起请求及后台运行内存中（验证：检查模板数据、任务配置、数据库、结果文件和日志，期望均无凭证值）。
- [ ] API Key 不出现在 URL、localStorage 或浏览器持久化状态（验证：发起测试后检查当前 URL、localStorage 和刷新后的表单，期望凭证字段为空且无凭证值）。
- [ ] 完整 Prompt、请求体和响应体不进入模板、任务详情、summary、details、报告或日志（验证：对结果目录和数据库内容执行敏感文本检索，期望无完整内容）。
- [ ] 错误信息不会回显 API Key 或完整供应商响应（验证：模拟 HTTP 错误、超时和非 JSON 响应，期望页面只显示脱敏后的短错误信息）。

## 集成

- [ ] 模板创建后可以无缝进入供应商自测发起流程（验证：模板列表点击“发起测试”，期望跳转自测页并带出模板配置）。
- [ ] 模板版本和模板启用状态在任务创建时被固化到任务配置，不受后续模板编辑影响（验证：创建任务后编辑模板，再查看原任务详情，期望原任务仍使用创建时配置）。
- [ ] 任务列表接口对旧任务和新模板任务都返回合法结构（验证：调用 `GET /api/tests`，期望三类任务均可解析且无 500 错误）。
- [ ] 本地 SQLite 和 Docker/PostgreSQL 配置都能完成模板表初始化（验证：SQLite 本地启动检查模板表；使用 Compose 数据库启动后检查后端健康接口和模板接口）。

## 编译与测试

- [x] 后端现有测试和新增供应商模板测试全部通过（验证：`.venv/bin/python -m pytest -q`，实际结果：118 passed）。
- [x] 后端新增和修改 Python 文件均可编译（验证：`.venv/bin/python -m compileall -q backend scripts`，实际结果：退出码 0）。
- [x] 前端生产构建成功（验证：`cd frontend && npm run build`，实际结果：构建成功，仅有第三方 `#__PURE__` 非阻断 warning）。
- [ ] 前端浏览器控制台无新增 error 级别错误（验证：打开模板列表、表单、自测页和历史页后检查控制台）。
- [x] API 健康检查正常（验证：`curl -sS http://127.0.0.1:8000/api/health`，实际结果：`{"status":"ok"}`）。

## 端到端场景

- [ ] 合法模板 -> 选择模板 -> 输入临时凭证 -> 启动非流式自测 -> 查看 Token/费用结论 -> 下载报告（验证：使用本地模拟供应商和参考接口完整执行，期望任务完成且各页面可访问）。
- [ ] 合法模板 -> 启用流式测试 -> 执行 SSE 自测 -> 查看 TTFT 和 usage 汇总（验证：模拟 SSE 返回 `data:` 与 `data: ` 两种格式，期望均能完成解析）。
- [ ] 停用模板 -> 尝试发起测试 -> 任务创建被拒绝（验证：接口返回明确模板状态错误，历史任务仍可查看）。
- [ ] usage 缺失或价格未验证 -> 查看结果 -> 显示无法核验（验证：结果页和 Case 明细页均不显示错误的通过或 0 费用）。
- [ ] 供应商输出 Token 与官方不同但输入 Token 一致 -> 查看结果 -> 输出差异为观察项且不单独判失败（验证：模拟接口返回不同输出 Token）。

## 结果记录

- 验收执行日期：
- 使用版本或提交：
- 通过项数量：
- 未通过项数量：
- 阻塞项及原因：
